<form method="post" action="{{ path('app_categorie_delete', {'id': categorie.id}) }}" onsubmit="">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <a>
        <button class="btn error">SUPPRIMER</button>
    </a>
</form>
