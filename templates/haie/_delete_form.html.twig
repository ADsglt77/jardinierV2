<form method="post" action="{{ path('app_haie_delete', {'id': haie.id}) }}" onsubmit="">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ haie.id) }}">
    <a>
        <button class="btn error">SUPPRIMER</button>
    </a>
</form>